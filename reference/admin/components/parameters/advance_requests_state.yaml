in: query
name: state
description: |
  Accepted states are DRAFT, APPROVAL_PENDING, APPROVED, REJECTED, PAID.<br>
  Supported operators are `eq`, `like`, `in`.<br>
schema:
  type: string
  example: in.(APPROVED)
