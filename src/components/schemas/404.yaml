type: object
properties:
  message:
    enum:
      - Resource not found
      - Employee not found
      - Dwolla customer not found
    type: string
    description: >
      This error means the requested resource was not found or the requester doesn't have sufficient access.
