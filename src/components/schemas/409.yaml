type: object
properties:
  message:
    enum:
      - Conflict
    type: string
    description: >
      This error means the request could not be completed due to a conflict with the current state of the resource.
