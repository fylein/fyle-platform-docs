recently_used_fields_out:
  type: object
  additionalProperties: false
  properties:
  
    user_id:
      $ref: '../../components/schemas/fields.yaml#/user_id'
    org_id:
      $ref: '../../components/schemas/fields.yaml#/org_id'
    project_ids:
      type: array
      items:
        type: string
      description: |
        list of last five recently used project ids
      example: ['123121']
    category_ids:
      type: array
      items:
        type: string
      description: |
        list of last five recently used category ids
      example: ['123121']
    cost_center_ids:
      type: array
      items:
        type: string
      description: |
        list of last five recently used cost center ids
      example: ['123121']
    locations:
      type: array
      items:
        type: string
      description: |
        list of last five recently used locations
      example: ['123121']
    end_locations:
      type: array
      items:
        type: string
      description: |
        list of last five recently used end locations
      example: ["Chalakudy, Kerala, India"]
    start_locations:
      type: array
      items:
        type: string
      description: |
        list of last five recently used start locations
      example: ["Cochin, Kerala, India"]
    vehicle_types:
      type: array
      items:
        type: string
      description: |
        list of last five recently used vehicle types
      example: ['two wheeler']
    currencies:
      type: array
      items:
        type: string
      description: |
        list of last five recently used currencies
      example: ['INR']      