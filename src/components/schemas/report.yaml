report_out:
  type: object
  additionalProperties: False
  properties:
    id:
      $ref: ./fields.yaml#/id_string
    org_id:
      $ref: ./fields.yaml#/org_id
    created_at:
      $ref: ./fields.yaml#/created_at
    updated_at:
      $ref: ./fields.yaml#/updated_at
    employee_id:
      $ref: ./fields.yaml#/id_string
    employee:
      $ref: ./employee.yaml#/employee_out_embed
    purpose:
      type: string
      description: purpose for creating the report
      example: Business trip to London
    currency:
      $ref: ./fields.yaml#/currency
    amount:
      $ref: ./fields.yaml#/amount
    tax:
      type: number
      description: |
        Tax value of the object.
      example: 18.23
      nullable: True
    state:
      type: string
      enum:
        - DRAFT
        - APPROVER_PENDING
        - APPROVER_INQUIRY
        - PAYMENT_PROCESSING
        - APPROVED
        - PAYMENT_PENDING
        - PAID
    num_expenses:
      type: integer
      description: |
        Number of expenses part of this report
      example: 3
#    is_verified:
#      type: boolean
#      description: |
#        Determines whether this report has been verified by a verifier
#    settlement_id:
#      $ref: ./fields.yaml#/id_string
#    settled_at:
#      $ref: ./fields.yaml#/created_at
#      description: |
#        When was the settlement completed.
#    is_physical_bill_submitted:
#      type: boolean
#      description: |
#        If physical has been collected for this report
#    physical_bill_submitted_at:
#      description: |
#        Signifies when this report was submitted.
#      $ref: ./fields.yaml#/created_at
    is_manually_flagged:
      type: boolean
      description: |
        Determines whether this report was flagged manually
    is_policy_flagged:
      type: boolean
      description: |
        Determines whether this report was flagged from a policy
    reimbursed_at:
      type: string
      format: date-time
      example: '2020-06-11T13:14:55.201598+00:00'
      description: |
        Signifies if and when this report was reimbursed.
      nullable: True
    approved_at:
      type: string
      format: date-time
      example: '2020-06-11T13:14:55.201598+00:00'
      description: |
        Signifies if and when this report was approved.
      nullable: True
    submitted_at:
      type: string
      format: date-time
      example: '2020-06-11T13:14:55.201598+00:00'
      description: |
        Signifies if and when this report was submitted.
      nullable: True
    claim_number:
      type: string
      description: |
        A unique human readable identifier for the report
      example: 'C/2021/02/R/907'
    source:
      type: string
      description: |
        where was this report object created from
    approvals:
      type: array
      items:
        $ref: ./report_approval.yaml#/report_approval_out_embed
      description: |
        List of report_approval objects
#    expense_ids:
#      type: array
#      items:
#        $ref: ./fields.yaml#/id_string
#      description: |
#        Ids of expenses that are part of this report
#    export_ids: # used in tpa_reports_view
#      type: array
#      items:
#        $ref: ./fields.yaml#/id_string
#      description: |
#        id of expenses that are part of this report
#    exported: # used in tpa_reports_view
#      type: boolean
#      description: |
#        Determines whether this report was flagged from a policy
