in: query
name: verified
schema:
  type: string
  example: eq.true
description: >
  verified is used to filter expenses by verified flag.

  For example, if verified is true, it will return all the verified expenses. If
  verified is false, it will return all the unverified expenses. leaving it
  blank will return all the expenses.
