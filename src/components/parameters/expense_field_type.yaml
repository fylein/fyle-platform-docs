in: query
name: type
description: |
  The type of the expense field
  
  Supported operators are `eq`, `like`, `in`.<br>
  field_name=eq.SELECT
schema:
  type: string
  example: eq.NUMBER
  enum:
    - TEXT
    - NUMBER
    - SELECT
    - BOOLEAN
    - MULTI_SELECT
    - LOCATION
    - USER_LIST
    - DATE
