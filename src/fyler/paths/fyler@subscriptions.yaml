post:
  tags:
    - Subscriptions
  summary: Webhook Subscribtions
  description: Webhook for different events can be subscribed using this API
  operationId: fyler-webhook-subscriptions
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          additionalProperties: false
          properties:
            data:
              $ref: '../../components/schemas/subscriptions.yaml#/subscriptions_in'
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            type: object
            additionalProperties: false
            required:
              - data
            properties:
              data:
                $ref: '../../components/schemas/subscriptions.yaml#/fyler_subscriptions_out'
    '401':
      description: Unauthorised request
      content:
        application/json:
          schema:
            $ref: '../../components/schemas/401.yaml'
