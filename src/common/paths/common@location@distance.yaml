post:
  tags:
    - Places
  security:
    - oauth2: ['*']
  summary: Get distance
  description: Returns the route distance in metres between the supplied coordinates.
  operationId: location_distance_post
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../../components/schemas/location.yaml#/location_route_in'
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            type: object
            additionalProperties: false
            required:
              - data
            properties:
              data:
                $ref: '../../components/schemas/location.yaml#/location_distance_out'
    '400':
      description: Bad request
      content:
        application/json:
          schema:
            $ref: '../../components/schemas/location.yaml#/location_error_out'
    '401':
      description: Unauthorized request
      content:
        application/json:
          schema:
            $ref: '../../components/schemas/401.yaml'
