post:
  x-internal: true
  tags:
    - Spender Onboarding
  summary: Process connect cards onboarding step
  description: |
    Marks the connect cards onboarding step as configured or skipped for the authenticated
    spender. Exactly one of `is_configured` or `is_skipped` must be provided. This step is
    only available for organisations with VISA or MasterCard card feed enrollment settings
    enabled, and can be re-attempted after being skipped, but not after being configured.
  operationId: spender_onboarding_process_step_connect_cards_post
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          additionalProperties: False
          required:
            - data
          properties:
            data:
              $ref: '../../components/schemas/spender_onboarding.yaml#/spender_onboarding_step_connect_cards_in'
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            type: object
            additionalProperties: False
            required:
              - data
            properties:
              data:
                $ref: '../../components/schemas/spender_onboarding.yaml#/spender_onboarding_step_connect_cards_out'
    '400':
      description: Bad request
      content:
        application/json:
          schema:
            $ref: '../../components/schemas/400.yaml'
    '401':
      description: Unauthorized request
      content:
        application/json:
          schema:
            $ref: '../../components/schemas/401.yaml'
    '403':
      description: Forbidden
      content:
        application/json:
          schema:
            $ref: '../../components/schemas/403.yaml'
    '404':
      description: No onboarding was found for this spender
      content:
        application/json:
          schema:
            $ref: '../../components/schemas/404.yaml'
