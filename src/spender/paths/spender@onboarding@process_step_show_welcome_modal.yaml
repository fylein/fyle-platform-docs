post:
  x-internal: true
  tags:
    - Spender Onboarding
  summary: Process show welcome modal onboarding step
  description: |
    Marks the welcome modal onboarding step as complete for the authenticated spender.
  operationId: spender_onboarding_process_step_show_welcome_modal_post
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          additionalProperties: False
          required:
            - data
          properties:
            data:
              $ref: '../../components/schemas/spender_onboarding.yaml#/spender_onboarding_step_show_welcome_modal_in'
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            type: object
            additionalProperties: False
            required:
              - data
            properties:
              data:
                $ref: '../../components/schemas/spender_onboarding.yaml#/spender_onboarding_step_show_welcome_modal_out'
    '400':
      description: Bad request
      content:
        application/json:
          schema:
            $ref: '../../components/schemas/400.yaml'
    '401':
      description: Unauthorized request
      content:
        application/json:
          schema:
            $ref: '../../components/schemas/401.yaml'
    '404':
      description: No onboarding was found for this spender
      content:
        application/json:
          schema:
            $ref: '../../components/schemas/404.yaml'
