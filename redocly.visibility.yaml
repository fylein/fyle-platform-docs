rules:
  rule/operation-x-internal:
    subject:
      type: Operation
      property: x-internal
    assertions:
      defined: true
      schema:
        type: boolean
    message: Every operation must explicitly set x-internal to true or false.
    severity: error
